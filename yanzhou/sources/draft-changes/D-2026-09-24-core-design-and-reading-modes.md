# 核心设计浓缩与探索阅读范围

日期：2026-09-24。状态：Accepted / Documentation。管理决定：G002-DOC-008。设计版本保持GDD 1.0 RC1 / doc.1。

用户明确要求补一份GDD概括浓缩的核心设计，并在启动新探索方向时控制模型所读游戏材料，支持GDD、核心设计和全量文档等方式。

落实内容：新增design/core-design.md，作为唯一成篇的核心设计浓缩；core-concept.md保留既有版本标识和兼容章节入口，不另维护一套规则摘要。浓缩依据来源提交0591fa5874ae5ed1b68e561a49737971cc9ede86的GDD、版本清单及系统正文，保留范围与Unknown，不增加玩法决定。

新增exploration/start.md统一定义CORE、GDD、FULL、CUSTOM及显式空白方式，给出材料边界、版本锁定、扩读规则、启动话术和方向内阅读记录。新方向未指定模式时使用CORE并先说明；旧方向续作沿原来源范围，不追溯改写其基线。FULL指全量主系统文档，历史、其他方向、外部代码和联网资料分别显式选择，不能靠链接自动扩读。

同步根路由、项目AGENTS、探索注册表、文档合同、导航和编号。每方向仍只需README；长来源清单仅在实际需要时同目录附文件，不恢复旧流程树。

检查核心设计与GDD事实对照、阅读模式的文件集合、链接、版本与旧规则冲突、既有未提交文件保护。目录与阅读规则采纳不代表玩法或体验验证；AUD-010保持Open。正式新玩法继续资格、提案/评估、目标Draft Change和明确采纳。

## 文档检查结果

- CORE游戏输入为1份浓缩；GDD固定清单16份均存在。FULL按当前目录与文本类型展开为378份主系统文本，集合包含完整GDD且不含历史、探索方向或其他项目；这里只验证集合，没有借检查模式读取全量正文。
- 新增核心约3812字符，逐节对应GDD的产品、循环、系统、成长与范围；核心标识和RC1玩法版本不变。
- 对本次22份文件检查本地链接和章节，未新增断链；来源版本字段与旧core-concept章节入口保留。旧27方向的正文和背景未改。
- 既有942条修改/删除不纳入本次，检查存在状态与可读文件哈希保持一致。只做文档检查，没有运行玩法测试。
