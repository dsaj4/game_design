# 实例绑定与条件绑定

状态：Qualified GDD Material / Accepted：SW02引用模型。证据：Hypothesis；逐次名单、排序及具体交互仍待设计。

## 素材控制

| 字段 | 内容 |
| --- | --- |
| 素材 ID | MAT-2026-09-10-instance-and-conditional-binding |
| 素材类型 | Mechanic / Constraint |
| 来源 inbox | [SW02问题、两种引用及用户确认](../idea-inbox/2026-09-10-semantic-world-object-scope.md) |
| 资格确认日期 | 2026-09-10 |
| 确认方式 | grill-with-docs；对照固定目标、SW01范围和单位身份后，用户确认两种绑定并存 |
| 当前证据状态 | Hypothesis；规则确认，无玩家测试 |
| 负责人 | 用户确定模型，agent维护规则及来源 |

## 规范化表述

法术对象引用支持实例绑定与条件绑定，两种方式并存。实例绑定在战前明确所指对象身份，后续释放持续尝试该对象；对象失效时跳过，不自动指向同名或其他对象。条件绑定在战前确定选择条件，每次释放按当前世界匹配符合条件的对象，符合条件的新生对象可以参与。

两种绑定均遵守法杖允许范围、已分配词卡的词义及对象当前合法性；匹配到对象不保证材料足够或效果可执行。词卡分配、绑定方式和选择条件在战前配置，战中不由玩家改写。具体实例的失效不会把该引用自动转为条件搜索。

单次释放的名单确定时点、名单内顺序及过程中新增或变化对象的处理，继续按SW02-A及后续细则设计。条件表达、空间变化、过程可介入时段与具体操作权限仍分别依赖SW03–SW07；数值继续重新设计。

| 引用意图 | 固定内容 | 每次释放的变化 |
| --- | --- | --- |
| 点名一个明确对象 | 对象身份 | 读取其当前状态；失效则跳过 |
| 选择符合条件的对象 | 战前配置的选择条件 | 匹配对象可以变化，也可以包括后来出现的对象 |

“选择条件”目前表示玩法上的引用意图，具体条件词表、表达界面以及能否混用、去重与数量限制均未确认。普通种类词不因此自动代表全部、最新或最强对象；实例绑定也不因同名替换而延续身份。

## 设计对象与 GDD 映射

- 作用对象：法术的对象引用与新生对象可用性。
- 目标 GDD：尚未创建。
- 建议章节：法术对象、目标选择与失效规则。
- 对应完成度：GDD-0 / GDD-1的引用模型素材；完整执行细则尚待确认。

## 玩家处境、行为与反馈

- 玩家处境：在战前配置循环法术，观察自动战斗。
- 玩家看见/知道什么：点名对象与按条件选择具有不同含义；新生对象可能符合已设条件。
- 玩家会做什么或改变什么决定：在表达允许时选择持续作用于特定对象，或作用于每次释放时符合条件的对象。
- 系统如何反馈：实例失效显示对应尝试无效；条件绑定的具体命中对象随世界求值，反馈展示方式待定。
- 希望产生的体验或设计价值：既能精确指向，又能通过预先规划作用于战中出现的内容；实际理解成本待验证。

## 与当前构思的关系

- 承接[SW01范围](M-2026-09-10-semantic-world-object-scope.md)，保持战前配置、词卡副本约束、共享释放槽及固定基础规则。
- [目标规则](M-2026-09-06-basic-sentence-targeting-and-locking.md)按引用模式区分：实例身份固定，条件匹配结果可变；不再把整场固定对象集合作为所有法术的共同限制。
- [C02](M-2026-09-10-accepted-design-interfaces.md)的逐对象材料与完整法术结果检查继续适用；实例既定顺序保留，条件名单排序细则待明确。
- 召唤的独立身份、失效不转移及不新增专属词卡约束继续适用；C03完整召唤流程仍后置。
- 明确不包含：本次名单锁定时点、同次连锁追加、排序、位移、任意词义或卡牌权限，以及战中手动重新配置。

## 机制—行为—体验假设

| Mechanics | Dynamics | Aesthetics |
| --- | --- | --- |
| 实例绑定不自动改指向 | 玩家维护特定对象的作用关系 | 精确、身份可追踪 |
| 条件战前固定，匹配随当前世界变化 | 玩家预设应对新生内容的法术 | 自动应对与表达空间 |

## 未知项与验证

| 未知项/风险 | 影响 | 下一步验证或决策方式 | 成功信号 | 失败信号 |
| --- | --- | --- | --- | --- |
| SW02-A名单时点 | 一次释放中对象可能产生或失效 | 比较开始时确定一次名单与过程中持续重选 | 能确定本次处理边界 | 名单无界增长或临时重选 |
| 排序与重叠引用 | 影响材料分配、重复作用 | 公开顺序方案与双引用重叠场景走查 | 结果唯一且可解释 | 依赖隐藏顺序或重复扣费 |
| SW03–SW05空间、句式与过程窗口 | 能匹配不代表能执行 | 分别补齐范围、表达和介入时机 | 代表句有合法引用及执行窗口 | 用可见性替代权限或只能操作演出 |
| 模型可读性 | 玩家可能误解同名替换 | 对照点名树消失、新树出现以及条件匹配 | 能解释两种引用的不同结果 | 认为实例自动转移 |
| 数量和成本 | 条件匹配可能扩大有效作用规模 | 按数值重设计任务建立输入与成本 | 多对象收益和成本有取舍 | 以本素材举例充当默认参数 |

## 资格确认清单

- [x] 原始表达、具体问题及“确认”可追溯。
- [x] 设计对象为两种引用模型及其并存关系。
- [x] 玩家处境明确为战前配置与自动战斗。
- [x] 玩家选择与可见影响明确。
- [x] 精确指向和条件应对的价值明确，体验待验证。
- [x] 与固定目标、C02及召唤身份关系明确。
- [x] 名单、排序等未知项有明确下一步，不将候选细则当作用户决定。

## 使用记录

| 日期 | GDD/Proposal | 章节/版本 | 处理结果 | 备注 |
| --- | --- | --- | --- | --- |
| 2026-09-10 | [引用提案](../idea-proposals/P-2026-09-10-instance-and-conditional-binding.md) | SW02 | Included | 两种绑定并存 |
| 2026-09-10 | [引用采纳](../draft-changes/D-2026-09-10-instance-and-conditional-binding.md) | G002-CORE-008 | Included | 名单、排序等细则仍待确认 |
