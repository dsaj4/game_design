# 第二流派：元素法术与状态替换

日期：2026-09-12。Project ID：game-002。当前状态：Promoted（明确机制方向与两次澄清）；执行缺项仍Unknown。正式素材：[元素法术规则方向](../idea-materials/M-2026-09-12-element-spell-archetype.md)。

## 原始想法

> 这一期设计效果都很好，先保留，但删除点燃等赋予状态动作，改用后续提到的元素法术；先直接做第二个流派的明确细化：第二个流派：元素法术 流派定位：玩家通过在战场上释放元素（例如：火焰），赋予其他附近单位对应状态（例如：燃烧），并操控元素进行其他动作（例如：攻击敌人、吞噬护甲等），特点是：1.元素释放：释放元素需要有对应空格，释放法术会先尝试在空格中生成元素，如果没有空格，则会尝试赋予其施法范围中单位对应状态（例如：燃烧），直到空出格子或施法结束；法术范围内已存在对应单位则不生成新单位，而是不断叠加已有单位层数2.元素存在：元素释放时会携带对应状态（例如：燃烧），会随时间流逝消失，层数为0时元素消失，由环境单位转化的元素会继承状态层数3.克制机制：火克草，草克冰，冰克火，场上单位可能自带对应属性（例如：大树是草属性，也存在无属性单位），和携带状态无关。被对应法术克制的单位状态，层数增加的速度翻倍，减少速度减半；克制对应法术的单位状态层数增加速度减半，减少速度翻倍4.状态机制补充：每个单位只能同时有一种元素状态，想要改变其元素状态，必须先消耗掉原有元素状态，不同元素之间等量消耗，但受克制影响 流派成型后续讨论

## 触发来源

本轮用户明确输入，接续[简易卡池](2026-09-12-simple-spell-creative-card-pool.md)及[第一人称格子战场素材](../idea-materials/M-2026-09-12-first-person-grid-battlefield.md)。当前第一人称战场GDD/BF-C为Draft Change；本输入不能被旧Parked范围否决，但也不自动采纳整个BF-C。

## 可能带来的玩家体验

玩家在战前围绕空格、敌人固有属性和元素已有位置配置法术；战中观察元素出现／叠层／消散、邻近单位的状态变化，以及先消耗旧状态再建立新状态的过程。流派成型由用户明确留待后续。

## 暂定标签

- 类型：元素流派机制／法术释放／格子占位／元素状态。
- 情绪：空间安排和状态变化可读是设计意图，体验证据Unknown。
- 玩法关键词：空格、同种叠层、满格回退、元素衰减、环境转化、火草冰克制、单元素状态、异种抵消。
- 风险关键词：持续过程与旧直接结算的衔接；层数是否同一量；转化身份；消减与自然衰减不能重复套克制。

## 资格确认记录

使用grill-with-docs查当前核心／词汇、格子战场素材、BF候选及FX-003／010／060，先完成可从文档判断的关系。

| 资格问题 | 当前答案 | 状态 |
| --- | --- | --- |
| 设计对象 | 第二流派的元素生成、存在、克制与状态替换 | Clear |
| 玩家处境／功能 | 依空位、已有元素和单位属性配置战前方案 | Clear |
| 玩家行为与可见影响 | 释放生成或叠层，满位仍施加状态，操控已在场元素；元素层数消退，状态相互抵消 | Clear：用户方向；部分执行含义待澄清 |
| 设计价值 | 场上元素与对象关系决定持续作用，提供有区别于单次直接效果的空间／状态选择 | Clear：由用户机制可见影响归纳，未证明体验 |
| 当前关系 | 复用战前配置、占格、范围、元素真实身份与状态；须协调BF独立寿命和环境烧焦候选、原单次名单与持续过程 | Clear：差异已定位 |
| 未知与下一步 | 先明确状态替换顺序，再明确本体与对应状态是否共享层数；成型后续讨论，不补平衡值 | Clear：决策路径 |

### 第一项确认：异种状态抵消

完整问题：“异种元素状态互相消耗时，是否按这个顺序：先根据新施加元素与目标固有属性的克制关系，修正本次施加量；再与旧状态逐层抵消，旧状态清空后的剩余量建立新状态？我推荐这样处理，避免一次转换重复计算克制。”

用户回答：“采用这个抵消顺序（推荐）”。此确认排除在同一次异种抵消中再对旧状态减层重复施加克制倍率；不自动定义独立自然衰减和其他消除动作的所有边界。

### 第二项确认：共享层数

完整问题：元素本体层数与它携带的对应状态层数是否同一个量；火焰只有一份燃烧层数，随时间衰减，归零消失，环境转化时直接继承这份层数。用户回答：“是，同一份层数（推荐）”。

当前关键缺项转为环境转化的条件、身份与固有属性，及元素宿主在异种抵消中归零时如何承接余量；本轮不擅补。

### 资格结论

- [ ] Ready for Material Review。
- [x] Promoted：用户方向、影响、关系和待决路径已清楚，限定机制方向晋级[素材](../idea-materials/M-2026-09-12-element-spell-archetype.md)。
- 具体草元素对应状态、元素主动攻击／吞噬的完整词义、环境转化条件与身份仍Unknown。

## 第一流派调整

用户要求保留本期创意效果。当前按“直接赋予元素状态的动作”落实：SC-V06点燃、SC-V07冰冻从活跃卡表删除；原ID保留为撤出记录，避免重用。有效候选48项：12名词、10动词、12形容词、14镶嵌。其余保留为候选，不把“保留”写成核心规则全部Accepted。

“获得护甲／力量／蓄能”和“标记”等非元素状态操作暂保留；删除范围按本轮元素语境解释，不反向删除护甲体系。不得用“获得燃烧／获得冰冻”等换名句式恢复元素状态直接施加入口。燃烧／冰冻名词和状态效果保留，由元素释放与元素作用负责施加；灼热／寒冷等已保留创意如何适配新入口，后续单独处理。

## 来源与效果登记

- 新FX-063：元素释放的生成／同种叠层／满位回退流程。
- 新FX-064：元素层数生命周期与归零消散。
- 新FX-065：环境转化元素并继承状态层数。
- 新FX-066：固有属性克制对元素状态增减的修正。
- 新FX-067：互斥元素状态的抵消与替换。
- 新FX-068：元素邻近施加对应状态的通用方向；火焰具体复用FX-060，草／冰实际词义不预填。
- FX-003／010增加本轮方向修订，FX-060记录与通用框架的关系；基础伤害FX-001、吞噬护甲FX-023仅作为后续操控案例，不在本轮补吞噬收益。

## 下一步

先确定共享层数及执行范围，整理明确规则与冲突接口；不开展流派成型、卡牌扩展、代码、平衡值或测试执行。用户提供的双倍／减半属于已明示机制，必须保留，不以“暂不做数值”省略。
